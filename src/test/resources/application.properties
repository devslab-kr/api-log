# Tests run with the BUILTIN strategy (the default in v0.3.0+), so this file
# is mostly here to be explicit. The starter creates the api_log table for
# us on first DataSource access via DataSourceScriptDatabaseInitializer.
api.log.schema.management=builtin
