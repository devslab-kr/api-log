# Tests need the api_log table to exist. v0.2.0 made schema management
# opt-in (default NONE), so for testing we explicitly turn on the bundled
# Flyway migration. Consumer apps choose for themselves.
api.log.schema.management=flyway
